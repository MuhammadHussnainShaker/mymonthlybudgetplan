import { Router } from 'express'

const router = Router()

// Phone-based authentication routes have been removed.
// All authentication now uses Firebase.
// See /api/v1/auth/bootstrap for user authentication.

export default router
