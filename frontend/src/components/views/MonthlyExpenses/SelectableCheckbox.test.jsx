import { describe, it } from 'vitest'

describe('SelectableCheckbox', () => {
  it.skip('component is preserved in codebase but not rendered in UI', () => {
    // The SelectableCheckbox component exists and is functional but is no longer
    // rendered in the main UI per product decision. The component code is kept
    // for potential future use. All tests are skipped as the component is not
    // accessible to end users.
  })
})
